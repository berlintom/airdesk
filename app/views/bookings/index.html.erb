<div class="container">
  <div class="header"><h2>Check out your bookings!</h2></div>
  <hr>
  <% @bookings.each do |booking| %>
    <div class="bookings">
      <li>
        <div class="bookings-index-content">
          <%= booking.start_date %>
          <div class="btn btn-normal"><%=link_to "see booking details", booking_path(booking) %></div>
        </div>
      </li>
      <hr>
  	</div>
  <% end %>
</div>
